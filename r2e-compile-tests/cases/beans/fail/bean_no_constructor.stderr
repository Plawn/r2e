error: #[bean] requires a constructor — a static method returning `Self` or `Result<Self, E>`:

         #[bean]
         impl MyService {
             fn new(dep: OtherService) -> Self {
                 Self { dep }
             }
         }

       A fallible one aborts boot naming the bean:

             fn new(dep: OtherService) -> Result<Self, BootError> { ... }
 --> cases/beans/fail/bean_no_constructor.rs:7:6
  |
7 | impl MyService {
  |      ^^^^^^^^^
