error: `env = ...` requires `app = <App type>` — the environment is handed to `App::build`, so there is nothing to boot it into:

         #[r2e::test(app = MyApp, env = ENV.get().await)]
 --> cases/testing/fail/test_env_without_app.rs:5:13
  |
5 | #[r2e::test(env = env())]
  |             ^^^^^^^^^^^
