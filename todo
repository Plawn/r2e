

- macro de service vs uniquement controller
- merge route controller et route methode -> WIP
- audit la partie data pour faire plus de reuse et du clean
- audit de performance
- audit de responsabilities boundaries 
- cli ?
- gestion de bean ? -> injection ? -> graph de dependance ? @ApplicationScoped ?
- gestion d'un inject generique dans le body pour faire transactionnal plus clean
- injecter l'identity dans les params handler et pas au niveau controller
- update doc -> DONE
- derive Service et pas que controller ?
- ecosystem tower ? quid ? ya quoi ? ça marche comment ?
- ecosstems de middleware ?
- register de scheduled -> avoir des bean de service et ça register auto
- audit ce qui a besoin d'etre send + sync
- est ce qu'on peut pas leak certains singleton pour gagner des perfos ?
- gestion de la secu plus flexible
- gestion des extensions plus flexible -> systeme de plugin with(impl plugin) pour Cors, tracing, etc
- routes en trop dans le openapi -> main route des controller (pas réellement de route, juste un prefix pour le reste)