[package]
name = "quarlus-scheduler"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["time", "rt", "sync"] }
tokio-util = { version = "0.7", features = ["rt"] }
cron = "0.13"
chrono = "0.4"
tracing = "0.1"
